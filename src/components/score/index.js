import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

const Score = () => (
  <>Score: 2048</>
);

Score.propTypes = {
  // width: PropTypes.number,
};

Score.defaultProps = {
  // size: 4,
};

export default Score;
